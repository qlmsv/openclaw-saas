/**
 * Docker Client wrapper that delegates to the external package
 */

export { 
  dockerClient, 
  DockerClient, 
  type ContainerInfo, 
  type UserContainerConfig 
} from "@openclaw-saas/openclaw-wrapper";
